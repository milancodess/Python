a = (1, 45, 567, False, "Hello", 3.14)  # Creating a tuple with mixed data types
print(type(a))  # Display the type of a, which is a tuple
print(a)  # Display the empty tuple

# a[0] = 100  # Attempting to change the first element of the tuple
# print(a)  # Display the tuple after attempting to change it
# Tuples are immutable, so the above line would raise a TypeError if uncommented
print(a[0])  # Accessing the first element of the tuple

